import java.util.*;

class SimpleInterest {
	public static void main(String args[])
	{
		float P = x.nextFloat(), R = x.nextFloat(), T = x.nextFloat();
		float SI = (P * T * R) / 100;
		System.out.println("Simple interest: " + SI);
	}
}
